{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default scaffold capability set. Runtime commands remain explicit Rust invokes.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
